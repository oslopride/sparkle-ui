import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';

import Box from './';

<Meta title="Sparkle|Box" component={Box} />

# Box

## A div with flexbox props

Use it anywhere you need a flex container.

<Preview withToolbar>
  <Story name="standard">
    <Box>Content</Box>
  </Story>
</Preview>

<Props of={Box} />
