import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';

import Button from './';

<Meta title="Sparkle|Button" component={Button} />

# Button

## It is a button

Use it where you want a button.

<Preview withToolbar>
  <Story name="standard">
    <Button>Button</Button>
  </Story>
</Preview>

<Props of={Button} />

## Usage

### Variants

Button has some variants, maybe.

<Story name="variants">
  <div>
    <Button>Button</Button>
    <Button>Button</Button>
    <Button>Button</Button>
    <Button>Button</Button>
  </div>
</Story>

### Playground

Experiment with this story with Knobs addon in Canvas mode.

<Story name="knobs" parameters={{ decorators: [withKnobs] }}>
  <Button onClick={action('clicked')} disabled={boolean('Disabled', false)}>
    Button
  </Button>
</Story>
